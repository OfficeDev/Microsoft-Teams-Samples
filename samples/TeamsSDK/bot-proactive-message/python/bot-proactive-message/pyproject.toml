[project]
name = "bot-proactive-message"
version = "0.1.0"
description = "Proactive message sample that demonstrates how to send messages to Teams users without them messaging first"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "microsoft-teams-apps==2.0.15",
    "microsoft-teams-api==2.0.15",
]
