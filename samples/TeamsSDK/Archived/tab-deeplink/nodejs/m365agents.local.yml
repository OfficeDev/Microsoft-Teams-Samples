# yaml-language-server: $schema=https://aka.ms/teams-toolkit/v1.2/yaml.schema.json
# Visit https://aka.ms/teamsfx-v5.0-guide for details on this file
# Visit https://aka.ms/teamsfx-actions for details on actions
version: v1.2

additionalMetadata:
  sampleTag: Microsoft-Teams-Samples:tab-deeplink-nodejs

provision:
  # Creates a new Microsoft Entra (AAD) app for authenticating users locally.
  - uses: aadApp/create
    with:
      name: tab-deeplink-aad
      generateClientSecret: true
      signInAudience: "AzureADMyOrg" # Single-tenant
    writeToEnvironmentFile:
      clientId: AAD_APP_CLIENT_ID
      clientSecret: SECRET_AAD_APP_CLIENT_SECRET
      objectId: AAD_APP_OBJECT_ID
      tenantId: AAD_APP_TENANT_ID
      authority: AAD_APP_OAUTH_AUTHORITY
      authorityHost: AAD_APP_OAUTH_AUTHORITY_HOST

  # Creates a Teams app
  - uses: teamsApp/create
    with:
      name: tab-deeplink${{APP_NAME_SUFFIX}}
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID

  # Configure single-tenant bot identity values used by the bot service.
  - uses: script
    with:
      run:
        echo "::set-teamsfx-env MICROSOFT_APP_TYPE=SingleTenant";
        echo "::set-teamsfx-env MICROSOFT_APP_TENANT_ID=${{AAD_APP_TENANT_ID}}";

  # Register the local bot with Bot Framework using the AAD app.
  - uses: botFramework/create
    with:
      botId: ${{AAD_APP_CLIENT_ID}}
      name: tab-deeplink
      messagingEndpoint: ${{BOT_ENDPOINT}}/api/messages
      description: ""
      channels:
        - name: msteams

  # Apply the AAD manifest to update the created AAD app
  - uses: aadApp/update
    with:
      manifestPath: ./aad.manifest.json
      outputFilePath: ./build/aad.manifest.${{TEAMSFX_ENV}}.json

  # Validate the manifest schema
  - uses: teamsApp/validateManifest
    with:
      manifestPath: ./appManifest/manifest.json

  # Build the Teams app package with latest env values
  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appManifest/manifest.json
      outputZipPath: ./appManifest/build/appManifest.${{TEAMSFX_ENV}}.zip
      outputJsonPath: ./appManifest/build/manifest.${{TEAMSFX_ENV}}.json

  # Validate the built app package
  - uses: teamsApp/validateAppPackage
    with:
      appPackagePath: ./appManifest/build/appManifest.${{TEAMSFX_ENV}}.zip

  # Apply the Teams app manifest to an existing Teams app in Developer Portal.
  - uses: teamsApp/update
    with:
      appPackagePath: ./appManifest/build/appManifest.${{TEAMSFX_ENV}}.zip

deploy:
  # Install npm dependencies
  - uses: cli/runNpmCommand
    with:
      args: install --no-audit

  # Generate runtime environment variables for the local run.
  - uses: file/createOrUpdateEnvironmentFile
    with:
      target: ./.env
      envs:
        TeamsAppId: ${{TEAMS_APP_ID}}
        MicrosoftAppId: ${{AAD_APP_CLIENT_ID}}
        MicrosoftAppPassword: ${{SECRET_AAD_APP_CLIENT_SECRET}}
        MicrosoftAppType: SingleTenant
        MicrosoftAppTenantId: ${{AAD_APP_TENANT_ID}}
        Base_URL: ${{BOT_ENDPOINT}}
