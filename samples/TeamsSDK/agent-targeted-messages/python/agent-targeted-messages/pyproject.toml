[project]
name = "targeted-messages-agent"
version = "0.1.0"
description = "This sample demonstrates targeted messaging in Microsoft Teams using Python — a reminder agent where all responses are private targeted messages."
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
    "python-dotenv>=1.0",
    "microsoft-teams-apps==2.0.15",
    "microsoft-teams-api==2.0.15",
    "microsoft-teams-common==2.0.15",
    "microsoft-teams-cards==2.0.15",
]
