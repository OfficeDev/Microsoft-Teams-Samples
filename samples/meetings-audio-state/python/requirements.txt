# Flask web framework
Flask==3.1.0

# WSGI HTTP Server for running Flask applications
Werkzeug==3.1.3

# Jinja2 templating engine (Flask dependency)
Jinja2==3.1.4

# MarkupSafe (Jinja2 dependency)
MarkupSafe==3.0.2

# Click (Flask dependency)
click==8.1.7

# Blinker (Flask dependency for signals)
blinker==1.9.0

# itsdangerous (Flask dependency for sessions)
itsdangerous==2.2.0
